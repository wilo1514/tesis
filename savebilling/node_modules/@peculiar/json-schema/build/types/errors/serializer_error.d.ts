import { JsonError } from "@peculiar/json-schema/build/types/errors/json_error";
export declare class SerializerError extends JsonError {
    schemaName: string;
    constructor(schemaName: string, message: string, innerError?: Error);
}
